<nav class="navbar navbar-expand-lg navbar-light bg-light navclass">
    <a class="navbar-brand" href="/">WikiLatic</a>
    <div class="collapse navbar-collapse" class="navbarNav">
        <ul class="navbar-nav">
            
            <% if(login && page) { %>
                <% if(page != "overall" && page != "individual" && page != "author") { %>
                    <li class="nav-item active">
                <%} else { %>
                    <li class="nav-item"></li>
                <% } %>
                        <a class="nav-link" href="/">Home</a>
                    </li>

                <% if(page == "overall") {%>
                    <li class="nav-item active">
                <%}else { %>
                    <li class="nav-item"></li>
                <%} %>
                    <a class="nav-link" href="/overall" id="overallAnalytics">Overall Analytics</a>
                    </li>
                <% if(page == "individual") { %>
                <li class="nav-item active">
                <%} else {%>
                    <li class="nav-item">
                <%}%>
                    <a class="nav-link" href="/individual" id='IndividualAnalytics'>Individual Article Analytics</a>
                </li>
                <% if(page == "author") { %>
                <li class="nav-item active">
                <% } else { %>
                    <li class="nav-item">
                <% } %>
                    <a class="nav-link" href="/author"> Author Analytics</a>
                </li>
            <% } else { %>
                <li class="nav-item active">
                        <a class="nav-link" href="/">Home</a>
                    </li>
            <% } %>
        </ul>
    </div>
    <!-- login and sign up div -->
    <div class="logclass">
        <% if(!login) { %>
            <% if(signupPage) { %>
            
            <% } else { %> 
            <a class="btn btn-outline-secondary my-2 my-sm-0" href="/signup">Sign up</a>
            <button class="btn btn-outline-secondary my-2 my-sm-0" type="submit" id="loginBtnNav">Log in</button>
            <% } %>
            
        <% }else{ %>
            <img src="/img/user-login-icon-14.png" class="logoperson">
            <span class="usernamespan">
                <%= username %>
            </span>
            <a class="btn btn-outline-secondary my-2 my-sm-0" id="logoutBtnNav" href="/logout">Log out</a>
        <%}%>
    </div>
</nav>